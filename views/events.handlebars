<h1>{{title}}</h1>
{{#each events}}
    <div class="events">
        <strong>{{this.title}}</strong><br>
        {{this.description}}<br>
        {{this.date}}<br>
        {{this.time}}<br><br>

        
    </div>
{{/each}}